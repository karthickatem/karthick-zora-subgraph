{
  "name": "karthick-zora-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ karthick-zora-subgraph",
    "create-local": "graph create --node http://localhost:8020/ karthick-zora-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ karthick-zora-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 karthick-zora-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
